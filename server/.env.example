GEMINI_API_KEY=
SERVER_PORT=5175

# Leave both empty for local dev (falls back to a local SQLite file at server/data/app.db).
# Set both to your Turso database's values to use a real persistent remote DB (required in production).
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
